{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "dev:watch": "concurrently -k -n server,vite -c cyan,magenta \"nodemon --watch app --watch routes --watch config --watch database --watch resources/views --watch bootstrap --ext php,blade.php --delay 1 --signal SIGTERM --exec \\\"php artisan serve --host=0.0.0.0 --port=8008\\\"\" \"vite\"",
        "prod": "npm run build && php artisan serve --host=0.0.0.0 --port=8008"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/vite": "^4.0.0",
        "autoprefixer": "^10.4.2",
        "concurrently": "^9.0.1",
        "laravel-echo": "^2.4.0",
        "laravel-vite-plugin": "^3.1",
        "nodemon": "^3.1.14",
        "postcss": "^8.4.31",
        "pusher-js": "^8.6.0",
        "tailwindcss": "^3.1.0",
        "vite": "^8.0.0"
    }
}
